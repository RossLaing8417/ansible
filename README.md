# Automating my life where possible

## Goodies that need to be run first
```command
sudo (apt|dnf|...) install ansible
```

The stuff is in the playbooks folder so I can use nvim plugin with the ansible lsp

